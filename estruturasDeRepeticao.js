//WHILE:

var empregados = ["Amanda", "Ailton", "Letícia"]

let i = 0;

while (i < empregados.length){
 console.log(empregados[i]);
 i++ ;  //sempre dentro, nunca fora das chaves
}

//================================================================================================

//FOR:

for(i = 0; i < empregados.length; i++){
    console.log(empregados[i]);
}